1️⃣ Install yang Dibutuhkan

Pastikan Python sudah ter-install (cek di CMD):

python --version


Lalu install library:

pip install pywebview pyinstaller

2️⃣ Buat File Python

Buat file app.py

import webview

webview.create_window(
    'Aplikasi Web Saya',
    'https://example.com',
    width=1200,
    height=800
)

webview.start()


Ganti https://example.com dengan link website Anda.

3️⃣ Compile Jadi .exe

Buka Command Prompt di folder app.py, lalu jalankan:

pyinstaller --onefile --windowed app.py

Arti perintah:

--onefile → jadi 1 file exe

--windowed → tanpa CMD hitam

4️⃣ Hasil File .exe

Setelah selesai, buka folder:

dist/


Di dalamnya ada:

app.exe


🎉 Itu sudah aplikasi desktop Windows